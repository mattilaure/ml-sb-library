import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    textInput : {
        width: 400,
        heigh: 180,
        backgroundColor: "white",
        borderRadius: 10,
        padding: 10,
        fontSize:20,
        fontFamily: "Arial"
    }
})