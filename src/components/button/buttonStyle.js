import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  buttonBasic: {
    width: 150,
    height: 50,
    backgroundColor: "orange",
    borderRadius: 10,
  },
  textBasic: {
    color: "white",
    fontSize: 30,
  },
});
