import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
   modalCustom: {
    backgroundColor: "#faf0dc",
    borderWidth: 5,
    borderColor: "#d5b895",
    maxWidth: 200,
    height: 200,
    gap: 20,
    
  },
  
});
